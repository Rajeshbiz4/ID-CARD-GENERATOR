LOGIN PAGE - FIT SCREEN / NO DESKTOP SCROLL

Replace only:
frontend/src/pages/LoginPage.jsx

Changes:
- Desktop login page now uses 100dvh.
- Vertical overflow is hidden on desktop.
- Main section uses flex: 1 with minHeight: 0.
- Header, content card and footer spacing reduced slightly.
- Login card is constrained to the available viewport.
- No browser vertical scrollbar should appear at normal desktop resolutions.
- Mobile/small screens still allow natural scrolling when content cannot fit.
- Login/authentication/API logic is unchanged.

After copying:
cd frontend
npm run dev

Then hard refresh:
Ctrl + Shift + R
